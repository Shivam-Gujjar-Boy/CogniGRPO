# Data Folder

This folder contains a small processed sample dataset used by the demo runner.

Structure:

- `processed/sample.jsonl` — tiny example dataset used by `src/demo.py`.

The demo uses deterministic, schema-correct samples so reviewers can run the
project quickly and see example evaluation outputs.
